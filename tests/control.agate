head
    title "Password Demo"
body
    input(#pass .mybutton type "password").submit
        if(length .value < 8)
            p(style "color:red; background-color:cyan") "Your password sucks"
        else
            script
                alert("Hooray!");